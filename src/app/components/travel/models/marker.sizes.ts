import { ISize, Size } from '../definitions/size';

export const AIRPORT_MARKER_SIZE: ISize = new Size(16, 16);
export const CURRENT_MARKER_SIZE: ISize = new Size(28, 28);
export const PORT_MARKER_SIZE: ISize = AIRPORT_MARKER_SIZE;
export const PREVIOUS_MARKER_SIZE: ISize = new Size(24, 24);
export const STATION_MARKER_SIZE: ISize = AIRPORT_MARKER_SIZE;
